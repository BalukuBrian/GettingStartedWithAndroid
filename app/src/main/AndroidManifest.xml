<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.balbrilabs.roomreservation">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RoomReservation">
        <activity android:name=".activities.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activities.NotificationView"
            android:label="@string/details_of_notification"
            android:parentActivityName=".activities.RegistrationActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activities.RegistrationActivity"/>
        </activity>

        <activity android:name=".activities.OrderService">
        </activity>

        <activity android:name=".activities.AddReservationActivity">
        </activity>

        <activity android:name=".activities.RegistrationActivity">
        </activity>

        <activity android:name=".activities.ShowReservationActivity">
        </activity>

        <activity android:name=".activities.SingleReservationActivity">
        </activity>

        <receiver android:name = ".broadcasts.MyReceiver">
            <intent-filter>
                <action android:name = "android.net.conn.CONNECTIVITY_CHANGE"
                    tools:ignore="BatteryLife" />
                <action android:name = "android.net.wifi.WIFI_STATE_CHANGED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>